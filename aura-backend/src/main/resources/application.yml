spring:
  application:
    name: aura-backend
  
  # MySQL Configuration
  datasource:
    url: jdbc:mysql://localhost:3306/aura_db?useUnicode=true&useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
    username: root
    password: ${DB_PASSWORD:123456}
    driver-class-name: com.mysql.cj.jdbc.Driver
  
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
        format_sql: true
  
  # OpenAI Configuration
  ai:
    openai:
      api-key: ${OPENAI_API_KEY}
      chat:
        options:
          model: gpt-4o-mini
          temperature: 0.7
          max-tokens: 1000
      embedding:
        options:
          model: text-embedding-3-large
          dimensions: 3072  # text-embedding-3-large supports up to 3072 dimensions
    
    # SimpleVectorStore Configuration
    vectorstore:
      simple:
        file-path: ./data/vector-store.json
        auto-save-interval: 60000

# Server Configuration
server:
  port: 8080
  error:
    include-message: always
    include-stacktrace: on_param

# CORS Configuration
cors:
  allowed-origins: http://localhost:5173,http://localhost:3000,http://127.0.0.1:3000,http://127.0.0.1:5173,http://127.0.0.1:3001
  allowed-methods: GET,POST,PUT,DELETE,OPTIONS
  allowed-headers: "*"
  max-age: 3600

# Logging Configuration
logging:
  level:
    root: INFO
    com.aura: DEBUG
    org.springframework.ai: DEBUG
    org.hibernate.SQL: DEBUG
  pattern:
    console: "%d{yyyy-MM-dd HH:mm:ss} - %msg%n"

# Application Configuration
app:
  pdf:
    manuals-path: classpath:manuals/
  vector:
    chunk-size: 800  # 增加分块大小以保留更多上下文
    chunk-overlap: 100  # 增加重叠以提高连续性
