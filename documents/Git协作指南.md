# Git协作指南 - Aura Quiet Living

## 🎯 拉取最新代码（安全流程）

### 第一步：保存本地进度
```bash
# 查看当前状态
git status

# 添加所有修改
git add .

# 提交到本地仓库
git commit -m "描述：保存我的本地进度"
```

### 第二步：拉取远程代码
```bash
# 拉取并合并
git pull origin master
```

### 第三步：处理冲突（如果有）
如果出现冲突，Git会提示：
```
CONFLICT (content): Merge conflict in xxx.java
```

**解决步骤**：
1. 打开冲突文件，查找 `<<<<<<<`, `=======`, `>>>>>>>` 标记
2. 手动选择保留哪部分代码
3. 删除冲突标记
4. 保存文件
5. 提交解决后的代码：
   ```bash
   git add .
   git commit -m "解决合并冲突"
   ```

### 第四步：推送到远程仓库
```bash
git push origin master
```

---

## 📦 本次更新内容（2026-02-02）

### 成员A的工作 ✅
- `OpenAIConfig.java` - AI配置
- `VectorStoreConfig.java` - 向量存储配置
- `PDFParser.java` - PDF解析工具
- `PDFVectorizationService.java` - PDF向量化服务
- `RAGService.java` - RAG问答服务
- `MultiAgentService.java` - 多智能体协调

### 成员F的工作 ✅
- **后端**：`ProductController.java`, `OrderController.java`, `ChatRequest.java`, `ChatResponse.java`, `OrderRequest.java`, `ApiResponse.java`
- **前端**：`api.ts`, `Assistant.tsx`, 集成ProductGrid和Checkout

### 产品说明书 ✅
- 6个PDF文件（位于 `aura-backend/src/main/resources/manuals/`）

---

## ⚠️ 注意事项

1. **永远先commit再pull** - 避免丢失本地修改
2. **遇到冲突不要慌** - Git会保留所有版本，手动选择即可
3. **提交信息要清晰** - 使用 `git commit -m "描述：做了什么"`
4. **定期拉取** - 每天开始工作前先 `git pull`，避免大量冲突

---

## 🔧 常用命令速查

```bash
# 查看状态
git status

# 查看修改内容
git diff

# 查看提交历史
git log --oneline

# 撤销未提交的修改
git restore <文件名>

# 查看远程仓库
git remote -v
```

---

## 📞 遇到问题？

1. 先用 `git status` 查看当前状态
2. 搜索错误信息
3. 联系团队负责人
